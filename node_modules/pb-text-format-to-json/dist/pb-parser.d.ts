import parsimmon from 'parsimmon';
export type ParseResult = parsimmon.Success<any> | (parsimmon.Failure & {
    error: string;
});
export type Values = Array<{
    type: 'pair';
    name: string;
    value: any;
} | {
    type: 'message';
    name: string;
    values: any;
}>;
export type JSONValue = string | number | boolean | null | JSONObject | JSONArray;
export interface JSONObject {
    [x: string]: JSONValue;
}
export interface JSONArray extends Array<JSONValue> {
}
export declare function parse(input: string, schema?: {}): JSONObject | {
    error: string;
};
